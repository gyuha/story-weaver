# 2026-08-20 — 작품 문체 지침(style_note): 필드·프롬프트 4곳 주입·시놉시스 UI

task 84 · part 1/2 · tdd: on · Dynamic Workflow(에이전트 4개, eco→sonnet, 37분, 381k 토큰)

## 계획 대비 실제

**계획대로 된 것**

- 네 슬라이스 전부 TDD로 착지하고 red를 실제 출력으로 인용했다(S1 `AttributeError: 'Work' object has no attribute 'style_note'`, S2 27 failed, S3 프론트 3 + 백엔드 2 failed).
- **`api/CLAUDE.md`의 마이그레이션 왕복 규약이 처음 적용됐고 효과가 확인됐다** — `"head"` 복원 + `finally`로 작성돼, 스위트 실행 후에도 `alembic current`가 `0007_work_style_note (head)`를 유지했다(공유 dev DB가 갇히지 않았다).
- 기존 작품에 백필하지 않았다(ADR 260813-110724 준수). 게이트 직접 재실행: 백엔드 1285 passed · errors 0, 프론트 398 passed.

**발산**

1. **S4(검증 담당)가 완료 기준을 잘못 판정했다 — 이 실행에서 가장 중요한 사건.** 계획의 S3 완료 기준은 *"브라우저에서 입력란이 보이고 저장 후 새로고침해도 값이 남는 것을 **눈으로** 확인"*인데, S4는 **RTL 테스트를 근거로 "충족"**이라 판정했다. 계획의 검증 노트 3번이 정확히 그 함정("UI 변경은 jsdom이 못 보는 것이 있다")을 경고했는데도 넘어갔다. 메인 세션이 UAT로 보완해 결과적으로 미충족은 없었다.
2. **S3가 저장 경로를 범용 `PATCH /api/v1/works/{work_id}`로 정했다**(계획이 S3에 맡긴 판단). 근거: `style_note`는 화풍처럼 카탈로그 검증에 얽히지 않은 평범한 Work 컬럼이고, `WorksService.update_work`가 이미 `model_dump(exclude_unset=True)` + `setattr`인 범용 로직이라 서비스 코드 변경이 0이다. 화풍의 전용 엔드포인트는 카탈로그 검증이라는 별개 책임 때문이지 "새 필드마다 전용화"가 아니다.
3. **삽입 위치를 문맥별로 다르게 했다** — 채팅·집필보조는 별도 문단, 기획의도·비트시트는 프롬프트 맨 끝의 별도 문장. 후자는 원본이 접속 어구로 이어지는 단일 문장이라 중간 삽입 시 문법이 깨진다. task 81의 마침표 겹침을 의식한 선택이다.
4. `assemble_prompt`에 키워드 기본값 `None`을 둬 기존 호출부·테스트가 아예 깨지지 않게 했다 — 계획이 "깨지는 테스트를 고쳐라"고 한 것보다 나은 처리.
5. `Work` 타입에 필드가 늘며 프론트 픽스처 15개 파일에 `styleNote: null`을 한 줄씩 넣었다(로직 변경 없음). 변경 파일 43개 중 상당수가 이것이라 diff가 실제 작업량보다 커 보인다.
6. 서버 `--reload`가 이번엔 걸렸다 — 지난 세션에서는 안 걸려 수동 재시작이 필요했으니, 그 현상은 항상 재현되지 않는다.

## 배운 것

**다음에 다르게 할 것**

- **검증을 위임할 때 완료 기준이 요구한 확인 수단을 프롬프트에 명시하고 대조를 요구할 것.** → **`CLAUDE.md`에 규약으로 승급했다.** 같은 세션에서 개입 효과까지 확인했다: task 85 프롬프트에 이 두 줄을 넣자 검증 담당이 "확인 수단 대조 메모"를 스스로 쓰고 배선 단절 red까지 확인했다.
- **양방향 테스트에서 "안 들어간다" 쪽이 실제 방어다.** S4가 조건 분기를 제거해 red를 확인했을 때 잡힌 것은 `문체 지침: None`이라는 **껍데기 주입**이었다. 포함 쪽만 테스트했다면 이것을 놓쳤다.
- **UAT에서 대조군이 생기면 증거가 훨씬 강해진다.** 같은 작품·같은 엔드포인트에서 지침 설정 전(미포함)/후(포함)를 `llm_call_logs`로 관측한 것이 목 테스트보다 결정적이었다. 앞으로도 "설정 전 호출"을 일부러 남겨 대조군으로 쓸 수 있다.
- **파일별 테스트 순감 대조를 하지 않았다** — 순증(+39/+3)만 확인했다. task 81이 `git show HEAD:`로 "사라진 테스트 0"을 단정한 것에 비해 한 단계 약하다.

## 문서 갱신

- CONTEXT.md 승급: 없음 — [[문체]]·[[문체 지침]]은 이 작업의 그릴링 단계에서 이미 등록했고, 실행 중 새 용어는 생기지 않았다.
- ADR 추가: 없음 — 범용 PATCH 선택은 되돌리기 쉽고(엔드포인트는 나중에도 추가 가능) 근거가 `run.md`에 있다.
- **`CLAUDE.md`에 "검증을 서브에이전트에 위임할 때" 섹션 추가** — 위 첫 항목.

## 후속 작업 후보

- 문체 지침이 실제 생성 품질을 개선하는지는 검증하지 않았다(계획의 비목표, 작가 판단 영역).
- QA 작품(`227a9754…`)에 검증용 `styleNote`와 화풍 `ink`가 남아 있다.
