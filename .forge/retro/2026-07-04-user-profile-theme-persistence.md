# 2026-07-04 — 프로필·테마 설정 서버 영속 + 로그인 시 테마 적용

## 계획 대비 실제
- 계획대로 된 것: S1~S6 전 조각이 설계대로 실현됨. 서버 영속(User.avatar_emoji·theme, PATCH /me), 계약 재생성, 프로필/테마 저장 배선, 로그인·세션 복원 시 서버 우선 테마 적용까지 DoD 충족.
- 차이(divergences):
  - S1·S2(마이그레이션+모델, PATCH 엔드포인트)가 이 fg-run 전에 이미 워킹트리에 구현돼 있었음 — 재구축 없이 검증만 수행. 설계 자체는 계획대로 실현됨(설계 미스 아님, 선행 작업 상태 차이).
  - `user-menu.tsx`(상단바 드롭다운)의 테마 선택은 여전히 로컬 전용(서버 미저장) — 이번 작업 범위(설정 화면 `ThemeSection`)에는 없었지만, 그 경로로 테마를 바꾸면 서버에 안 남아 다른 세션에서 서버 우선 적용에 덮인다.
  - 계약 재생성으로 `avatar_emoji`/`theme`가 필수 필드가 되어 기존 테스트 mock 2곳 갱신 필요.

## 배운 점
- 다음에 다르게 할 것: 특별히 없음 — 이번 작업 방식(eco 상한 + 경량 2단계 파이프라인, S4·S5 web-feature-builder 위임)은 잘 맞았고 다른 접근이 필요 없었음.
- 후속 작업 후보: `user-menu.tsx` 상단바 테마 선택도 서버 저장 경로로 연결(설정 화면 `ThemeSection`과 동일한 persist-on-select 적용) — 별도 태스크로 그릴링 권장.

## 문서 업데이트
- CONTEXT.md 승급: 없음 (UI 구현 세부사항, 도메인 용어 아님)
- ADR 추가: 없음 (하드-투-리버스·트레이드오프 성격의 결정 없음)
