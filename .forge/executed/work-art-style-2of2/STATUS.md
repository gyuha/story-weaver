# STATUS — 작품 화풍 (2/2): `이미지 스타일` 메뉴·화면과 카드 생성 영역 정리
slug: work-art-style-2of2
status: executed
executed: 2026-08-13
verified: yes (코드 게이트 + **브라우저 UAT 6단계 전부 통과**.
  게이트: `pnpm typecheck` 클린 · `pnpm lint` 클린(biome 239파일) · `pnpm test` **56 files / 394 passed**. 테스트 순감을 `git show HEAD:` 대조로 파일별 확인 — 380 → 394(+14): `art-style-screen.test.tsx` +11(신규) · `work-shell.test.tsx` 1→2(+1) · `entity-image-section.test.tsx` 10→12(+2). **사라진 테스트 없음.** 방어 제거 red 확인 4건(확인창 분기 · 화풍 미변경 조기 반환 · 견본 `onError` 대체 · 미지정 유도 분기).
  UAT(`:3001` 웹 + `:8000` API, 사용자의 `:3000` 세션은 건드리지 않았다 — 다른 오리진이라 QA 계정 주입이 아무것도 침범하지 않는다): ① 내비 `이미지 스타일`(4번째) → 화풍 4개가 각각 견본 3장과 함께, **12장 전부 렌더·깨진 것 0** ② 현재 화풍이 `aria-pressed=true`, 톤 입력에 저장값 유지 ③ 카드 상세에 **템플릿 그리드 0개**, `이 작품의 화풍` 한 줄, 스트립·대표·묘사 유지 ④ 웹에서 `생성` → `template_id=ink-character`, **`is_primary=f`**(두 번째가 대표를 바꾸지 않음), `final_prompt` 2050자가 **시각 묘사 → 작품 톤 → 화풍 → 구도** 순, **눈으로 확인**해 수묵화·흰 배경·짙푸른 자톤·종이 질감이 모두 반영되고 이전 이미지와 같은 인물 ⑤ 화풍 변경 시 확인창 **"이미 만든 이미지는 그대로 남고, 앞으로 생성하는 이미지만 새 화풍을 따릅니다"**, `취소` 후 **DB가 `ink` 그대로**(창이 떴다가 아니라 PUT 미발생을 DB로 단정) ⑥ 화풍 없는 작품에서 `생성` 버튼 **없음** + 유도 문구.
  **부수로 #80이 남긴 견본 불일치를 닫았다** — `oil` 견본 2장을 재생성해 16/16, 둘 다 눈으로 확인(배경이 새 규칙과 일치). 겸해서 #80이 "실행 검증 안 함"으로 남긴 샘플 재생성 스크립트가 실제로 동작함을 확인했다.)
retro: pending

## 남은 형태 문제 하나 (기능 결함 아님)
화풍 카드가 콘텐츠 전체 폭을 쓰는데 견본 3장이 왼쪽에만 몰려 **오른쪽이 크게 빈다**(스크린샷 확인). 그릴링 목업은 좁은 카드에 52px 썸네일이었고 실제 폭은 훨씬 넓다 — jsdom이 관측할 수 없어 계획이 "UAT에서 드러날 지점"으로 예고한 그것이다. 다듬는 방법 셋: 견본을 키우거나 · 카드 폭을 제한하거나 · 4화풍을 2×2로 배치. **사람이 보고 정할 일이라 이번에 임의로 바꾸지 않았다.**

## 실행 중 확인한 별개 사실
`@*.test` 도메인 계정은 `/api/v1/auth/me`가 **500**을 내 웹 로그인이 불가능하다(pydantic `EmailStr`이 `.test`를 예약 TLD로 거부). 실제 사용자 이메일에는 영향이 없으나 이 저장소의 QA 잔여 계정 다수가 그 도메인이다. UAT를 위해 계정 하나를 `qa-artstyle@example.com`으로 바꿨고 되돌리지 않았다. 앞으로 QA 계정에 `.test`를 쓰지 말 것.
