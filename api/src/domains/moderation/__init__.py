"""moderation 도메인 — LLM 호출 실패 분류 (ADR `260730-070532`).

**콘텐츠 수위를 판정하지 않는다.** 과거에는 전체이용가 상한을 강제하는 키워드 선제
가드(S1)와 완화 재시도(S2)를 담았으나, ADR `260730-070532`으로 제품이 강제하는
연령·수위 제한을 전부 제거했다 — 정책 집행은 모델 제공자에게 위임한다. 도메인 이름은
리네임 비용을 피해 그대로 두었다.

남은 책임은 LLM 호출 실패를 **운영 실패**(인증·연결·레이트리밋 등 → 502
``LLMUnavailableError``)와 **제공자 거절**(빈 응답·기타 예외 → 정직한 거절 안내)로
갈라 사용자 대면 처리를 정하는 것이다. 어느 경우든 provider의 raw 에러는 노출하지 않는다.
"""
