"""dynamic_update 도메인 — 씬 저장 시 신규 설정 후보 추출 파이프라인(ai-pipeline.md 7장).

M3 동적 업데이트 파이프라인(plan.md M3-S1/S2/S3): 씬 본문 + 링크된 엔티티 카드를
LOW_COST 티어 LLM에 넘겨 신규 엔티티/속성 변경/타임라인 상태 변화 후보를 JSON으로
추출하고(S1), 기존 엔티티와 매칭해 노이즈를 걸러낸 뒤 제안으로 저장하고(S2), 승인 시
엔티티/타임라인 상태에 반영하거나 거절 시 폐기한다(S3). 웹 배선(S4)은 후속 슬라이스.
"""
